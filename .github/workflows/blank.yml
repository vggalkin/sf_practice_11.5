name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  sql:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: ls ${{ github.workspace }}
    - run: cat ../rfam.sql
          
