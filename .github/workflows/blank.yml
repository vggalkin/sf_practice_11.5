name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  sql:
    runs-on: ubuntu-latest
    steps:
    - run: ls ${{ github.workspace }}
    - run: cat ${{ github.workspace }}/rfam.sql
          
